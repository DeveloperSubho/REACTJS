/**
 * Object.entries(Object)
 * The Object.entries() method returns the entries (entries()) as an array. It returns a nested array of the following form:
 * [[key, value], [key2, values2], …]
 *
 */
console.log(Object.entries({ id: 1, name: "Manuel" }));
