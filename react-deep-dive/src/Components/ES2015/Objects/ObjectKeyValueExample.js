/**
 * Object.keys(Object)
 * The Object.keys() method returns the properties (keys) as an array.
 *
 */
console.log(Object.keys({ id: 1, name: "Manuel" }));

/**
 * Object.values(Object)
 * The Object.values() method offers similar functionality to Objects.keys(). It returns the values (values) as an array.
 */
console.log(Object.values({ id: 1, name: "Manuel" }));
